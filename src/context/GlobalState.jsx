import React, {createContext, useEffect, useReducer} from 'react';

// initial state
const initialState

const GlobalState = () => {
    return (
        <div>
            
        </div>
    );
};

export default GlobalState;